@inherits DisplayBase

<PageComponent Name="Assignable Value Display" EditingNode="EditingNode">
    <Publish>
        @if(ComponentData is not null)
        {
            if(ComponentData.RequireAuth)
            {
                <AuthorizationHandler Settings="ComponentData">
                    <Authorized>
                        @ComponentData.AuthorizedMarkup
                    </Authorized>
                    <NotAuthorized>
                        @ComponentData.UnAuthorizedMarkup
                    </NotAuthorized>
                </AuthorizationHandler>
            }
            else
            {
                @ComponentData.AuthorizedMarkup
            }
        }
        else
        {
            <em>Invalid component data registration.</em>
        }
    </Publish>
    <Configure>
        <PageComponentEditorSettingsNode Name="Value Configuration">

        </PageComponentEditorSettingsNode>
    </Configure>
</PageComponent>