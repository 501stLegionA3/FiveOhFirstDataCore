<div class="grid grid-cols-2 m-2 gap-2">
    @foreach(var pos in Slots)
    {
    <div class="bg-blue-600 p-1 text-center font-bold">@pos.Name</div>
    <div class="bg-gray-800 p-1 text-center font-bold">@(pos.OccupiedBy?.NickName ?? "TBD")</div>
    }
</div>