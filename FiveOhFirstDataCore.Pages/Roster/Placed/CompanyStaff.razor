@using FiveOhFirstDataCore.Core.Data.Roster
@using FiveOhFirstDataCore.Core.Data
@inherits RosterModel

<tbody>
    <tr role="@(Company.Commander is null ? "" : "button")" @onclick="(x) => TrooperNav(Company.Commander?.Id)">
        <th scope="row" colspan="2" class="text-end">
            Company Commander
        </th>
        <td colspan="2" class="text-start">
            @if (Company.Commander is null)
            {
                <span>TBD</span>
            }
            else
            {
                @Company.Commander.DisplayRankName()                
            }
        </td>
    </tr>
    <tr role="@(Company.XO is null ? "" : "button")" @onclick="(x) => TrooperNav(Company.XO?.Id)">
        <th scope="row" colspan="2" class="text-end">
            Company Executive Officer
        </th>
        <td colspan="2" class="text-start">
            @if (Company.XO is null)
            {
                <span>TBD</span>
            }
            else
            {
                @Company.XO.DisplayRankName()                
            }
        </td>
    </tr>
    <tr role="@(Company.NCOIC is null ? "" : "button")" @onclick="(x) => TrooperNav(Company.NCOIC?.Id)">
        <th scope="row" colspan="2" class="text-end">
            Company NCOIC
        </th>
        <td colspan="2" class="text-start">
            @if (Company.NCOIC is null)
            {
                <span>TBD</span>
            }
            else
            {
                @Company.NCOIC.DisplayRankName()                
            }
        </td>
    </tr>
    <tr role="@(Company.Medic is null ? "" : "button")" @onclick="(x) => TrooperNav(Company.Medic?.Id)">
        <th scope="row" colspan="2" class="text-end">
            Company Medic
        </th>
        <td colspan="2" class="text-start">
            @if (Company.Medic is null)
            {
                <span>TBD</span>
            }
            else
            {
                @Company.Medic.DisplayRankName()                
            }
        </td>
    </tr>
    <tr role="@(Company.RT is null ? "" : "button")" @onclick="(x) => TrooperNav(Company.RT?.Id)">
        <th scope="row" colspan="2" class="text-end">
            Company RT
        </th>
        <td colspan="2" class="text-start">
            @if (Company.RT is null)
            {
                <span>TBD</span>
            }
            else
            {
                @Company.RT.DisplayRankName()                
            }
        </td>
    </tr>
    <tr role="@(Company.ARC is null ? "" : "button")" @onclick="(x) => TrooperNav(Company.ARC?.Id)">
        <th scope="row" colspan="2" class="text-end">
            Company ARC
        </th>
        <td colspan="2" class="text-start">
            @if (Company.ARC is null)
            {
                <span>TBD</span>
            }
            else
            {
                @Company.ARC.DisplayRankName()                
            }
        </td>
    </tr>
    <tr role="@(Company.Adjutant is null ? "" : "button")" @onclick="(x) => TrooperNav(Company.Adjutant?.Id)">
        <th scope="row" colspan="2" class="text-end">
            Company Adjutant
        </th>
        <td colspan="2" class="text-start">
            @if (Company.Adjutant is null)
            {
                <span>TBD</span>
            }
            else
            {
                @Company.Adjutant.DisplayRankName()                
            }
        </td>
    </tr>
</tbody>

@code {
    [Parameter]
    public CompanyData Company { get; set; }
}
