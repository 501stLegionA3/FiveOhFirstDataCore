window.Util = (() => {
    return {
        getWindowDimensions() {
            return {
                width: window.innerWidth,
                heigh: window.innerHeight
            }
        }
    };
})();